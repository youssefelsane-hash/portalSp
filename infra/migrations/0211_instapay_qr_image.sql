-- baytak — 0211: QR كود InstaPay مُدار من الأدمن (docs/08 §78-د).
--
-- **طلب مالك صريح (2026-08-27)**: «حاول تخلّص الجزء بتاع الـQR بتاع الإنستاباي… عادي يبقى
-- الـQR ليه لينك مثلاً أو بيترفع من عن طريق الأدمن وخلاص… زيه زي بقية حاجات الدفع».
-- (كان مؤجّلًا بطلبه هو في §76: «مش دلوقتي لما أجيب الحاجات الحقيقية».)
--
-- **إعداد واحد بمصدرين، مش إعدادين**: القيمة إما `storage://<key>` (ملف اترفع من لوحة الأدمن)
-- أو رابط `https://...` خارجي، وفاضي = مفيش QR (شاشة العميل بتعرض التعليمات النصية بس زي
-- ما هي دلوقتي). خانتين منفصلتين (رابط + ملف) كانت هتخلق سؤال «مين يكسب لو الاتنين مليانين؟»
-- بلا أي مكسب — وده نفس فخ `login_logo` اللي اتشال في migration 0210 بالظبط.
--
-- **ليه مش `branding_assets`؟** ده مش أصل هوية بصرية للمنصة، ده **بيانات تشغيل دفع**: بيتغيّر
-- مع الحساب البنكي، بيقرأه نفس الكود اللي بيقرأ `ipa_address`/`recipient_name` (اللي هما
-- إعدادات من migration 0151)، ولازم يتقفل بنفس صلاحية `settings.manage` (super_admin بس).

INSERT INTO settings (key, value, value_type, group_name, description, is_public) VALUES
  (
    'payments.instapay.qr_image',
    '""',
    'string',
    'payments',
    'صورة QR لاستقبال تحويلات InstaPay — إما "storage://<key>" لملف مرفوع من لوحة الأدمن، أو رابط https خارجي. فاضي = مفيش QR (العميل بيشوف تعليمات التحويل النصية بس)',
    false
  )
ON CONFLICT (key) DO NOTHING;
