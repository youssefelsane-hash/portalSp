# baytak — سياق سريع لأي سيشن جديدة

اقرأ الملف ده الأول قبل أي حاجة. لو عايز التفاصيل الكاملة: `docs/01-master-plan.md` (الخطة) و`docs/02-data-dictionary.md` (قاموس البيانات + عقد الـ API). القاعدة الحاكمة: أي تغيير في اسم جدول/عمود بعد اعتماد `02-data-dictionary.md` لازم migration موثّق. لو محتاج تفعّل أي خدمة خارجية حقيقية (Paymob، S3، FCM/Twilio/SMTP، Google Maps): `docs/03-external-integrations.md` — الكود جاهز 100% لكل واحدة فيهم، الملف ده بيقولّك بالظبط منين تجيب كل قيمة وتحطها فين.

## ⭐ المبدأ الحاكم لكل شغل على المشروع ده (طلب صريح من المالك، 2026-08-11 — يسري على كل أكاونت/سيشن بلا استثناء)

**الشغل لازم يتم دايمًا باحترافية — أبداً بسرعة أو بكروتة.** كل إضافة لازم تكون:
- **مكتوبة ومدروسة قبل التنفيذ** — أي طلب/متطلب جديد من المالك يتسجّل فورًا في مكان مكتوب بالاسم قبل ما يتلمس أي كود (شوف `docs/08-pricing-engine-and-platform-vision.md` — ده الـ backlog الحي لكل الطلبات الجديدة الكبيرة).
- **مبنية بمعايير شركات عالمية** — أمان (security) مضبوط، بنية قابلة للتوسّع وسهلة الصيانة لعشرات السنين قدام، مش حل سريع مؤقت.
- **موثّقة بالاسم** — كل جزء جديد له اسم واضح ومكان واضح (موديول/جدول/service)، عشان أي AI/مطوّر بعدك يلاقي الاسم ده في التوثيق ويكمل عليه، **مش ينشئ نسخة تانية من حاجة موجودة أصلاً**. قبل ما تبدأ أي مهمة كبيرة: دوّر في `docs/08-pricing-engine-and-platform-vision.md` وREADMEs الموديولات الأول — لو لقيت الحاجة مكتوبة كـ"خلصت"، اعتبرها خلصت وكمّل اللي بعدها.
- **بمراحل واضحة** — قرار معماري كبير (schema جوهري، حدود موديولات جديدة) يتكتب كـ ADR في `docs/adr/` **قبل** التنفيذ مش بعده (القالب موجود في `docs/adr/README.md`).
- كود التعليقات عربي، وبس لما السبب مش واضح من الاسم نفسه — نفس القاعدة تحت، مذكورة هنا مرتين عمدًا لأهميتها.

**تحديث مهم (2026-08-11) — رؤية جديدة من المالك لسه بتتنفذ**: اقرأ `docs/06-vision-brief-sanaa.md` (رؤية كاملة لهيكل حجز جديد + براند "صُنّاع" + بيانات قياسية/إنتاجية) و`docs/07-multi-agent-task-split.md` (تقسيم التنفيذ لأربع أجزاء بين السيشنز/الأكاونتات المختلفة اللي بتشتغل بالتبادل على المشروع ده — **اقرأه قبل أي تنفيذ من `docs/06` عشان تعرف إيه اللي محجوز وإيه اللي فاضي**).

**تحديث أهم (2026-08-11، لاحقًا نفس اليوم) — رؤية تانية أكبر لسه بتتنفذ، فوق رؤية `docs/06`**: اقرأ `docs/08-pricing-engine-and-platform-vision.md` — محرك تسعير ديناميكي كامل (الأهم)، Scheduler حقيقي للفني، بحث/اختيار فني قبل الحجز، فرق عمل، ضمان وإعادة زيارة، تتبع لحظي، تقييم متقدم، وأكتر. **الملف ده هو نقطة التتبع الحية لكل ده — أي سيشن جديدة تحدّثه أول ما تبدأ/تخلص جزء، بالظبط زي `docs/07`.**

## إيه اللي موجود فعلاً دلوقتي (مش خطة — كود شغال ومختبر)

**`apps/api`** (NestJS + PostgreSQL/PostGIS + Redis + BullMQ) — **~15,000 سطر، 20 موديول، 32 migration، شغال ومختبر حي على Postgres/Redis حقيقيين.** كل الـ12 نقطة اللي كانت مطلوبة (RBAC هرمي، إدارة الموظفين، إدارة الفنيين، مستويات الفنيين، الفرق/الشركات، الكتالوج الديناميكي، المناطق/الفروع، لوحة تحكم موسّعة، audit log، الإشعارات، محرك الإعدادات، البنية event-driven بالـ queues/caching) لها كود شغال. الأمر: `cd apps/api && npm run start:dev` (يحتاج Postgres+Redis شغالين، راجع `infra/docker/docker-compose.yml`).

**`apps/admin`** (Next.js 16 + shadcn/ui) — شاشات أساسية شغالة ومختبرة (Playwright ضد الباك-إند الحقيقي): موظفين، فنيين، طلبات، كتالوج، إعدادات، سجل نشاط، عارض صور الطلب. تفاصيل كاملة في `apps/admin/README.md`.

**`apps/customer-app`, `apps/technician-app`** (Flutter) — **كود شغال حقيقي، مش README فاضي** — auth بالـ OTP، طلبات (إنشاء/تنفيذ/إلغاء بسبب+رسوم/تقييم)، دفع من المحفظة، شات وتتبع لحظي (Socket.IO)، رفع صور، أرباح/صرف للفني. **Flutter SDK متاح فعلاً في البيئة دي على `/opt/flutter/bin` — مش في `PATH` افتراضياً، لازم `export PATH="$PATH:/opt/flutter/bin"` الأول.** اختبارات حية حقيقية في `test_live/` لكل التطبيقين (`flutter test test_live/ --dart-define=API_BASE_URL=http://localhost:3000/api/v1`) — تفاصيل كاملة في الـ READMEs بتاعتهم. **تحديث (2026-08-12): رندر الـ widgets الفعلي وتفاعل اللمس بقى قابل للاختبار حقيقةً** عبر `flutter build linux` + `Xvfb` + `fluxbox` (window manager **ضروري** لموثوقية `xdotool`) — منهجية كاملة + بَقّة حقيقية اتلقطت ومتصلحة (`flutter_secure_storage.write()` بيفشل بصمت ويعلّق تسجيل الدخول) في `apps/customer-app/README.md`. `apps/technician-app` عنده فحص أمان (`CompromisedDeviceScreen`) بيمنع التشغيل في بيئة زي دي عمدًا — تفاصيل في README بتاعه.

**`packages/shared-types`** — أنواع TypeScript مشتركة بين `apps/api` و`apps/admin` و`apps/customer-web`، **وكمان ثوابت وقت-تشغيل حقيقية** (`PRICING_METHODS`, `PRICING_MODEL_LABELS`, `FORMULA_LIMITS`, `BRANDING_ASSET_LABELS_AR`…). `main` بيشاور على `dist/` وهي مستبعدة من Git، فأي export قيمة جديدة بيوصل بـ`git pull` من غير `dist` متبنية = `undefined` وقت التشغيل في المتصفح (`tsc` بيعدّي لأن Next dev مابيعملش typecheck). ده كان سبب بَقّة `/catalog` الحقيقية في 2026-09-01. اتقفل بنيويًا: `predev`/`prebuild`/`pretypecheck` في `apps/admin` و`apps/customer-web` بيبنوا الحزمة تلقائيًا، و`prepare` بيبنيها مع أي `npm install`. لو بتعدّل والـdev server شغال: `npm run build:watch` في `packages/shared-types`. التفاصيل في `packages/shared-types/README.md`.

## فجوات موثّقة صراحة (مش سهو — دوّر عليها بـ `grep -rn "فجوة موثّقة" apps/api/src --include="*.md"`)

أهمها: بَقّة BullMQ Worker reconnection بعد انقطاع Redis طويل (موثّقة بالتفصيل في `apps/api/src/modules/technicians/README.md` — مطابقة لـ GitHub issue #4479 في BullMQ نفسه، مش حاجة تتصلح من كودنا). باقي الفجوات أصغر (endpoints ناقصة، خوارزميات يدوية مؤقتاً).

## اتفاقيات ثابتة (من `docs/01-master-plan.md` §1.3)

- أسماء الجداول/الأعمدة: `snake_case`. الكود: `camelCase`. الـ API routes: `kebab-case`.
- كل الأسعار بالقرش (`integer`, مش `float`). كل الأوقات UTC. كل جدول فيه `id, created_at, updated_at, deleted_at`.
- Migrations: SQL خام في `infra/migrations/`، `synchronize:false` دايماً، ما تعدّلش migration اتعمل commit — دايماً ملف جديد برقم تالي.
- كود التعليقات: عربي، وبس لما السبب مش واضح من الاسم نفسه (مش شرح "بيعمل ايه").

## طريقة العمل المتّبعة في السيشنز اللي فاتت (اتّبعها)

1. مفيش توقف للتأكيد بين المهام — اختار المهمة الجاية، ابنيها، اختبرها حي (مش mocks) على Postgres/Redis حقيقيين شغالين فعلاً، لو لقيت بَقّة حقيقية اتعامل معاها، وثّق بصراحة (حتى الفجوات والقصور)، commit + push، كمّل.
2. أي فشل في cache/queue/infra لازم يتلقّط ويتعامل معاه بأمان (يرجع null/يسجّل تحذير/يرجع للـ DB) — أبداً ميكسرش أو يعلّق العملية الحقيقية للمستخدم. الدرس المكلف من السيشن ده: `queue.add()` كانت بتعلّق طلب حقيقي (تقييم/دفع) لدقايق وقت انقطاع Redis قبل ما نكتشف ونصلح.
3. قبل أي commit: `npx tsc --noEmit` ثم `npx nest build` ثم `npx jest` في `apps/api` — الثلاثة لازم يعدّوا نضيف.
4. كل موديول له `README.md` بيوثّق القرارات والفجوات بصراحة — حدّثه لما تلمس الموديول.

## فريق العمل الحالي

فرع Git: `claude/home-services-app-plan-v13gb2`. لا تعمل push على فرع تاني من غير إذن صريح.
