# 11 — قرارات المالك النهائية: RBAC ديناميكي + ترشيح QR للفني + KPI شهري + مسار وظيفي (2026-08-13)

**القاعدة الحاكمة**: نفس مبدأ `docs/08`/`docs/10` — الملف ده نقطة تتبع حية، يتحدّث لحظيًا وقت التنفيذ،
مش بعده. اقرأه قبل أي تنفيذ من الأربع أجزاء تحت.

## الطلب الأصلي من المالك (قرارات عمل نهائية، بالحرف تقريبًا — بالإنجليزي الأصلي محفوظ في المحادثة)

المالك حسم أربع نقاط كانت مؤجّلة في `docs/10` (البنود 38، 39، 42، 43) بقرارات منتج نهائية:

1. **تعويض التأخير التلقائي (بند 38) — ملغي صراحة من الإصدار الأول.** مايتعملش خالص دلوقتي.
2. **ترشيح QR للفني + مكافأة (بند 39) — مطلوب بالكامل end-to-end.**
3. **KPI شهري للفني — نظام جديد** (مش من `docs/10` الأصلي، طلب جديد): المنصة بتحسب نقاط أداء
   موضوعية وتعرضها، **الأدمن هو اللي بيقرر المبلغ النهائي** جوّه حدود قابلة للإعداد — مش قرار آلي.
4. **محرك المسار الوظيفي/الترقي (بند 42) — مبني على أداء حقيقي**، عتبات كل مستوى قابلة للإعداد
   بالكامل من الأدمن، مفيش رقم مكتوب في الكود. الترقي ممكن يبقى "مؤهّل" تلقائيًا بس التفعيل
   الفعلي يحتاج موافقة أدمن إلا لو الأدمن فعّل ترقي تلقائي لانتقال معيّن صراحة.
5. **باني أدوار ديناميكي / RBAC مرن (بند 43) — أولوية عالية، لازم ADR قبل أي كود** لأنه بيلمس
   بنية التفويض الأساسية.

**قاعدة عامة صريحة تحكم الأربعة**: **مفيش قيمة مالية أو حد أو وزن KPI أو عتبة مكتوبة في الكود
(hardcoded) خالص** — كل حاجة قابلة للتعديل من `/settings` أو من جدول قواعد مخصوص. الأمثلة
المذكورة (5000 جنيه سقف KPI شهري مثلاً) هي **قيم افتراضية معقولة**، مش أرقام نهائية.

**ترتيب التنفيذ المطلوب صراحة**: (1) ADR + باني الأدوار، (2) ترشيح QR + مكافأة الفني،
(3) محرك KPI الشهري + موافقة/صرف الأدمن، (4) محرك المسار الوظيفي. **كل جزء معزول، موثّق،
مختبر، commit ومدموج (merge) على حدة** — مش دفعة واحدة في نهاية الشغل كله.

**متطلبات عامة لكل الأربعة (مش استثناء)**: Backend + migrations + واجهة أدمن + واجهة عميل/فني
لما تكون مطلوبة + إشعارات + audit logs + دفتر مالي (wallet/ledger) + idempotency + صلاحيات
+ اختبارات سلبية + اختبار حي E2E. **مفيش اعتبار ميزة "خلصت" لمجرد إن الـ API موجود.**

## سجل التقدّم

- **2026-08-13 (بداية المرحلة)**: الملف اتعمل، الطلب الكامل اتسجّل بالحرف قبل أي كود — حسب
  المبدأ الحاكم في `CLAUDE.md`. البدء الفعلي: بحث معماري شامل في نظام RBAC الحالي (موديول
  `employees`/`auth`/الأدوار/الصلاحيات) قبل كتابة الـADR.

- **2026-08-13 (§4 باني الأدوار الديناميكي — ✅ خلص بالكامل، `ADR-0010`)**: البحث المعماري كشف
  إن نظام RBAC الحالي **مش hardcoded enums زي ما كان متوقّع** — 4 جداول حقيقية (`roles`/
  `permissions`/`role_permissions`/`user_roles`) شغالة من `migration 0003`، وأهم حاجة: **الـJWT
  مايحتويش على أدوار/صلاحيات خالص** (`{sub, userType}` بس) — يعني كل تغيير في الأدوار بياخد
  مفعوله فورًا بلا أي استراتيجية إضافية لـsession invalidation، النقطة اللي المالك طلب حل ليها
  صراحة كانت محلولة بالفعل بالتصميم الأصلي. الفجوة الحقيقية كانت في طبقة الـCRUD فوق الـschema،
  مش الـschema نفسه.
  - `migration 0081`: `roles.is_active` (تعطيل عكسي، فوري)، `roles.is_super_admin` (يقفل بَقّة
    حقيقية اتكشفت أثناء البحث — `super_admin` كان بيفتقد أي صلاحية جديدة بصمت لو حد نسي يمنحها
    له صراحة في الـmigration، موثّقة في `0038` نفسها — دلوقتي بيتخطى الفحص بالكامل بنيويًا).
  - `PermissionsService` اتوسّعت: CRUD كامل للأدوار (إنشاء/تعديل/نسخ/حذف)، إدارة صلاحيات الدور
    (مصفوفة كاملة)، منع تصعيد صلاحيات (`roles.grant_unrestricted`)، حماية القفل الذاتي (منفصلة
    عن حماية آخر `super_admin` الموجودة)، حماية الأدوار النظامية الخمسة من الحذف/التعطيل/تغيير
    الاسم (أسماؤها مستخدمة كـstring literals في قواعد التوجيه الافتراضية).
  - `apps/admin`: شاشتين جداد (`/roles`, `/roles/:id`) — قايمة، إنشاء، مصفوفة صلاحيات مجمّعة
    حسب الموديول، نسخ، تفعيل/تعطيل، حذف، المستخدمين الحاملين للدور، سجل تدقيق.
  - اتعمله اختبار حي كامل عبر curl (كل قواعد الأمان الخمسة: منع اسم مكرر، حماية دور نظامي،
    منع تصعيد صلاحيات، حماية القفل الذاتي، bypass الـsuper_admin) + Playwright حقيقي ضد
    المتصفح (لقطات شاشة أكّدت المصفوفة بتعرض بيانات `finance` role الحقيقية صح). الفحوصات
    الأربعة (tsc/nest build/jest في `apps/api`، tsc/next build في `apps/admin`) عدّت كلها.
    تفاصيل كاملة في `docs/adr/0010-dynamic-role-builder.md` و
    `apps/api/src/modules/admin/README.md` § باني الأدوار الديناميكي.

- **2026-08-13 (§2 ترشيح QR للفني + مكافأة — ✅ خلص بالكامل)**: `migration 0082` —
  `technician_referral_attributions` (إسناد دائم، أول استخدام بيربح) + `technician_referral_bonuses`
  (`order_id` UNIQUE = idempotency)، 8 إعدادات تحت `group_name='referral_qr'` (تفعيل، قيمة
  المكافأة، أقل حالة طلب مؤهّلة، وضع المكافأة أول-طلب/كل-طلب، حد أدنى لقيمة الطلب، سقف شهري
  للفني، تهدئة زمنية، رفض الجهاز المكرر) — **صفر قيم مكتوبة في الكود**، كل حاجة قابلة للتعديل.
  - `TechnicianReferralsService.evaluateOrderForBonus()` بيتنفّذ على `ORDER_STATUS_CHANGED_EVENT`
    الموجود أصلاً (مفيش حدث جديد)، وبيعمل double-entry حقيقي عبر `WalletsService` الموجود
    (محفظة المنصة → محفظة الفني) + `reverseDoubleEntry()` للإلغاء عند إلغاء/استرداد الطلب.
  - **بَقّة حقيقية اتلقطت واتصلحت وقت الاختبار الحي**: فحص "الجهاز المكرر" الأول كان بيقارن
    `user_devices` اللايف، لكن `device_id` فريد على مستوى الجدول (الملكية بتتنقل بين المستخدمين)
    فالفحص كان no-op دايمًا رياضيًا. اتصلح بعمود صورة (`customer_device_id` snapshot) على كل
    صف مكافأة، والمقارنة بقت ضد الصور المخزّنة مش الحالة اللايف. اتأكد حي بسيناريو 3 عملاء/جهاز
    واحد: مكافأة أولى `credited`، تانية `rejected_suspicious` بصفر أثر على المحفظة.
  - **UI كامل الأربعة**: `apps/technician-app` (`ReferralScreen` — QR عبر `qr_flutter`، مشاركة
    عبر `share_plus`، إحصائيات)، `apps/customer-app` (كود ترشيح اختياري في التسجيل + شاشة
    "عندي كود ترشيح" للعميل الحالي)، `apps/admin` (`/technician-referrals` — جدول + إجمالي لكل
    حالة، قراءة فقط لأن القرار كله آلي حسب الإعدادات).
  - الفحوصات الأربعة عدّت كلها (`tsc`/`nest build`/`jest` في `apps/api`، `tsc`/`next build` في
    `apps/admin`، `flutter analyze` نضيف في التطبيقين). تفاصيل كاملة في
    `apps/api/src/modules/technician-referrals/README.md`.
