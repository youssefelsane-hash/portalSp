# baytak — لقطة تقدّم كاملة (لو رجعنا بعد إجازة)

**تاريخ آخر تحديث لهذا الملف**: 2026-08-09. **فرع Git**: `claude/home-services-app-plan-v13gb2` (126+ commit، من 2026-08-06 لحد دلوقتي).

اقرأ الملف ده الأول لو رجعت بعد فترة ونسيت السياق. بعده اقرأ `CLAUDE.md` (سياق سريع)، وبعدين `docs/01-master-plan.md`/`docs/02-data-dictionary.md` لو محتاج تفاصيل أعمق. الملف ده **حالة لحظية** وقت كتابته — كل قسم فيه محدّث لحد آخر commit مذكور فوق، لو كملت شغل بعده حدّث الملف ده تاني في الآخر.

---

## 1. الوضع بالأرقام (نظرة سريعة)

| المقياس | القيمة |
|---|---|
| مدة الشغل الفعلية | 3 أيام (6-9 أغسطس 2026) |
| عدد الـ commits على الفرع | ~126 |
| أسطر كود TypeScript في `apps/api/src` (بدون اختبارات) | ~20,962 |
| أسطر اختبارات Jest في `apps/api/src` | ~356 |
| أسطر TypeScript/TSX في `apps/admin/src` | ~8,023 |
| أسطر Dart في `apps/customer-app/lib` | ~3,200 |
| أسطر Dart في `apps/technician-app/lib` | ~2,326 |
| أسطر اختبارات حية (`test_live/`) في التطبيقين | ~1,645 |
| أسطر TypeScript في `packages/shared-types/src` | ~968 |
| أسطر SQL في `infra/migrations` | ~1,938 عبر 42 migration |
| عدد موديولات الباك-إند | 21 |
| عدد ملفات README التوثيقية (موديولات + تطبيقات) | 21+ |
| **إجمالي تقريبي لكل الكود المكتوب فعلياً** | **~40,000 سطر** (كود + اختبارات + migrations، مش عدّاد دقيق لأن فيه كود توليد/generated files مستبعد) |

**نسبة التقدّم التقريبية** (تفاصيل كاملة في §5 تحت): **الباك-إند ~95%** من كل حاجة كانت مطلوبة تقنياً، **apps/admin ~95%** (كل الشاشات المخطط لها موجودة ومختبرة)، **apps/customer-app و apps/technician-app ~85-90%** (المسارات الأساسية كلها شغالة، ناقص بس ميزات أمان إضافية للفني قبل الإطلاق الحقيقي + تكاملات خارجية محتاجة مفاتيح حقيقية). **تكاملات خارجية**: الكود جاهز 100% للخمسة كلهم (Paymob, FawryPay, S3, FCM/Twilio/SMTP, Google Maps) لكن **صفر منهم عنده مفاتيح حقيقية شغالة دلوقتي** — كله log-only/رافض بوضوح لحد ما تتحط القيم الحقيقية (تفاصيل كل واحد فين تجيبه بالظبط في `docs/03-external-integrations.md`).

---

## 2. إيه اللي شغال فعلاً (مش خطة — كود مبني ومُختبر حي)

### 2.1 `apps/api` (NestJS + PostgreSQL/PostGIS + Redis + BullMQ)

**21 موديول، كل واحد فيهم شغال ومختبر حي على Postgres/Redis حقيقيين** (مش mocks — كل اختبار اتعمل بـ curl/Jest/Playwright/flutter test ضد سيرفر شغال فعلي، وأي بَقّة حقيقية اتلقطت اتوثّقت بصراحة في الـ README بتاع الموديول). الموديولات:

`admin` (RBAC هرمي، لوحة تحكم، dashboard analytics)، `audit` (سجل نشاط)، `auth` (OTP، JWT، refresh rotation)، `catalog` (فئات/خدمات/تسعير مناطق ومستويات/إضافات)، `chat` (شات الطلب + شات الدعم العام)، `customers`، `feature-flags`، `geo` (مدن/مناطق/نطاقات خدمة + رسم مضلّعات)، `matching` (توزيع الطلبات على الفنيين، BullMQ delayed jobs)، `notifications` (FCM/Twilio/SMTP dispatchers + توجيه حسب الدور)، `orders` (دورة حياة الطلب الكاملة + عروض الأسعار الوسيطة)، `payments` (Paymob + FawryPay + محفظة + صرف)، `payouts`، `pricing`، `promotions` (أكواد خصم + نقاط ولاء)، `ratings`، `settings` (محرك إعدادات ديناميكي)، `support` (شكاوى + تذاكر دعم)، `technicians` (اعتماد/مستويات/شركات وفرق/مناطق عمل)، `users`.

**التشغيل**: `cd apps/api && npm run start:dev` (يحتاج Postgres+Redis، `infra/docker/docker-compose.yml`). **قبل أي commit جديد**: `npx tsc --noEmit && npx nest build && npx jest` — الثلاثة لازم يعدّوا نضيف (كانوا نضيف آخر مرة اتشغّلوا، commit `770f1c6`).

### 2.2 `apps/admin` (Next.js 16 + shadcn/ui)

**كل الشاشات المخطط لها موجودة ومختبرة حي (Playwright ضد باك-إند حقيقي)**: نظرة عامة، موظفين، فنيين (+ مناطق عمل + مسار تحقق وسيط)، عملاء، طلبات (+ استرجاع + بنود عرض سعر + إعادة تعيين فني)، تقارير، شكاوى (+ تصنيف خطورة قابل للتعديل)، تذاكر دعم، **محادثات الدعم (جديد، آخر commit)**، طلبات صرف (+ تفاصيل بنود)، أكواد خصم (**+ تقييد بخدمات/مناطق، جديد**)، أسباب إلغاء، توجيه إشعارات، شركات/فرق فنيين، سياسة مستويات فنيين، كتالوج (+ تفاصيل خدمة كاملة)، مدن ومناطق (+ رسم مضلّعات تفاعلي بـ Leaflet)، إعدادات، Feature Flags (+ استهداف صريح)، سجل نشاط.

### 2.3 `apps/customer-app` و`apps/technician-app` (Flutter)

**كود شغال حقيقي، مش تطبيق فاضي** — auth بالـ OTP، تصفح كتالوج + إضافات اختيارية، إنشاء طلب، تتبع لحظي بخريطة Google Maps حقيقية، شات (نص + صور + **شات دعم عام، جديد**)، دفع (محفظة + بطاقة WebView + **فوري بكود مرجعي، جديد**)، تقييم، إلغاء بسبب+رسوم، عروض سعر وسيطة (موافقة/رفض)، **إشعارات push مُهيّأة (جديد، محتاجة مفاتيح Firebase حقيقية بس)**. الفني: نفس دورة الطلب كاملة، ملاحة خارجية، كشف أجهزة مكسورة (root/jailbreak).

**Flutter SDK**: `/opt/flutter/bin` (مش في PATH افتراضياً). اختبارات حية حقيقية في `test_live/` لكل تطبيق.

---

## 3. الشغل اللي اتعمل في الجلسة دي تحديداً (السياق المباشر قبل الإجازة)

بالترتيب الزمني، كل واحد منهم commit منفصل ومختبر حي بالكامل ومدفوع للفرع:

1. **`support_chat` (شات دعم عام)** — `GET /chat/support-thread` (عميل، get-or-create)، `GET /admin/support-chat-threads` (قايمة أدمن)، `resolveParticipant` اتوسّع يقبل أي أدمن عنده `support_tickets.manage` كطرف في أي خيط دعم عام. شاشتين جداد في `apps/admin` (`/support-chat`, `/support-chat/:id`)، `ChatScreen.support()` جديد في `apps/customer-app` + زرار دعم في الشاشة الرئيسية. اختبار حي كامل (curl + Playwright + `test_live/support_chat_live_test.dart`).
2. **تقييد أكواد الخصم بخدمات/مناطق** — الباك-إند كان جاهز بالكامل من زمان، بس مش معروض في فورم `/promotions`. أضيف قسمين checkbox، اتأكد حياً من الـ DB.
3. **تكامل عميل FCM** — `firebase_core`/`firebase_messaging` في التطبيقين، `PushNotificationService` جديد بيسجّل الجهاز بعد تسجيل الدخول (`POST /devices` الموجود من زمان). فشل صامت مقصود من غير ملفات Firebase حقيقية، بلجن Android شرطي عشان الـ build ميتكسرش. اتأكد حياً إن الـ try/catch شغال فعلاً (نداء مباشر لـ`registerCurrentDevice`، مش مجرد افتراض).

كل التفاصيل الكاملة لكل نقطة موجودة في الـ README بتاع الموديول المعني (`apps/api/src/modules/chat/README.md`, `apps/api/src/modules/promotions/README.md`, `docs/03-external-integrations.md` §4.1) وفي `apps/admin/README.md`/`apps/customer-app/README.md`.

---

## 4. طلب المستخدم الأخير (اللي شغّال عليه دلوقتي وقت كتابة الملف ده)

المستخدم طلب 4 حاجات في رسالة واحدة، بالترتيب اللي هتلاقيه شغال عليه (لو الجلسة اتقطعت، دي نقطة الاستكمال بالظبط):

1. **✅ الملف ده نفسه** — مطلوب يتكتب الأول قبل أي حاجة تانية (طلب صريح).
2. **⏳ نقاط الولاء (loyalty) قابلة للتعديل من السيستم**: "الفلوس بتتحسب بكم نقطة؟ خليها flexible... حط الخانة في السوبر أدمن، وهو اللي بيحدد... كتجربة حط أرقام متوسطة، وكده هي قابلة للتغيير". **القرار المتّخذ**: إعدادين جداد في محرك الإعدادات الموجود (`settings` table، مُدار من `/settings` في `apps/admin`):
   - `loyalty.earn_points_per_100_egp_spent` = 1 (نقطة واحدة لكل 100 جنيه — يعني نقطة تسجّل لكل عملية إنفاق ضمن نطاقات الـ100 جنيه)
   - `loyalty.point_value_cents` = 100 (كل نقطة = 1 جنيه لو اتصرفت)
   هي أرقام "متوسط الصناعة" مؤقتة زي ما طلب المستخدم بالظبط — **قابلة للتعديل فوراً من `/settings` في لوحة الأدمن من غير أي كود جديد** (نفس آلية باقي الإعدادات: `SettingsService.getNumber()` بيقرا القيمة كل مرة، مفيش قيمة متجمّدة في الكود). هيتربط بـ `LoyaltyService.earn()` تلقائياً عند اكتمال أي طلب مدفوع (كانت فجوة موثّقة صراحة إن الرقم مش موجود فالكود مش هيخترعه — دلوقتي الرقم بقى موجود وقابل للتعديل، فالفجوة اتقفلت).
3. **⏳ توسيع صلاحية شات الدعم**: "عايز دايماً السيستم يوصل تحت الأدمن... المانيجر يعرف يكلم مع العميل". **القرار المتّخذ**: منح صلاحية `support_tickets.manage` (نفس الصلاحية اللي شات الدعم بيستخدمها فعلاً) لدور `ops_manager` كمان (كانت بس `super_admin`+`support_agent`) — migration جديدة، مش تغيير في الكود. أي مدير عمليات بقى يقدر يرد على شات الدعم زي الأدمن الأعلى بالظبط.
4. **⏳ الأدمن يشوف المحفظة**: "عنده access إنه يخش يشوف المحفظة عشان يشوف هل في مشكلة". **القرار المتّخذ**: `GET /admin/wallets/:userId` جديد (قراءة بس، مفتوح لأي أدمن — نفس نمط قراءة تفاصيل العميل/الفني الموجود)، ويتعرض في صفحتي `/customers/:userId` و`/technicians/:id` في `apps/admin`.
5. **⏳ نظام تواصل داخلي (مدير↔فنيين، أدمن↔فنيين)**: ميزة جديدة تماماً — شات داخلي منفصل عن شات دعم العملاء، بين أي أدمن (موظف) وأي فني، أو بين موظفين. **القرار المتّخذ**: جدول جديد منفصل (`internal_chat_threads`/`internal_messages`, مش تعديل على `chat_threads` الموجود لأنه `customer_id NOT NULL` بيمنع استخدامه لمحادثة موظف↔فني من غير كسر قيد موجود على جدول مُختبر). نطاق أول نسخة: أدمن↔أدمن، أدمن↔فني — **مش** فني↔فني (مش مطلوب صراحة، ونطاق أوسع من كده كان هيحتاج قرارات هرمية إضافية مؤجلة).

**علامات ⏳ فوق يعني لسه بيتنفّذ وقت كتابة السطر ده** — راجع §6 (الخطوات الجاية) لتعرف بالظبط فين وقفنا، وحدّث القسم ده لـ✅ كل ما تخلص واحدة.

---

## 5. تقييم الاكتمال التفصيلي مقابل الخطة الأصلية

`docs/01-master-plan.md` بيغطي حاجات برّه نطاق الكود (كيان قانوني، تسويق، توظيف، تصميم براند) — دي مش شغلي كـ coding agent وملهاش نسبة اكتمال هنا. الجزء التقني (الموديولات §2.4 + السبرنتات التقنية S1-S12 في §6):

| الجزء | الحالة | ملاحظات |
|---|---|---|
| **البنية التحتية** (Docker, migrations, RBAC, audit) | ✅ 100% | 42 migration، صفر تعديل على migration اتعمل commit |
| **الكتالوج والتسعير** (فئات/خدمات/مناطق/مستويات/إضافات) | ✅ 100% | شامل CRUD كامل من apps/admin |
| **إنشاء ومطابقة الطلبات** | ✅ 100% | BullMQ delayed rounds حقيقية، مش cron بسيط |
| **دورة تنفيذ الطلب (تطبيق الفني)** | ✅ 100% | الحالات كلها + استرجاع تلقائي بعد إغلاق التطبيق |
| **الشات والتتبع اللحظي** | ✅ 100% | شات طلب + شات دعم عام + صور + خريطة حقيقية |
| **الدفع والفاتورة** | ⚠️ 95% | 3 بوابات (محفظة/بطاقة/فوري) الكود جاهز، محتاجين مفاتيح حقيقية للاختبار الكامل |
| **التقييم والشكاوى والصرف** | ✅ 100% | تصنيف خطورة تلقائي + تعديل يدوي |
| **لوحة الإدارة** | ✅ ~97% | كل الشاشات المخطط لها موجودة، آخر إضافة: محفظة الأدمن (شغالة دلوقتي) |
| **الإشعارات** | ⚠️ 90% | الديسباتشرز جاهزة، عميل FCM جاهز، محتاج مفاتيح Firebase/Twilio/SMTP حقيقية |
| **الأمان الأساسي** (RBAC, JWT rotation, OTP) | ✅ 100% | |
| **أمان الإنتاج المتقدم** (SSL pinning, mock-location detection) | ❌ 0% عمداً | محتاج شهادة TLS إنتاج حقيقية + ربط GPS حقيقي الأول — موثّق كقرار مؤجل مش سهو |
| **التواصل الداخلي (مدير/فنيين)** | ⏳ قيد البناء الآن | راجع §4 نقطة 5 |
| **نظام الولاء بمعدل حقيقي** | ⏳ قيد البناء الآن | راجع §4 نقطة 2 |

**الخلاصة الصريحة**: لو الهدف "منصة شغالة تقنياً بالكامل جاهزة تتوصل بمفاتيح حقيقية وتنزل"، إحنا في مرحلة متأخرة جداً (~90-95%) من الجزء التقني. **اللي فاضل بعد المهام الأربعة في §4 مش حجم كبير من الكود** — أساساً: (1) مفاتيح خارجية حقيقية (مش شغل كود، شغل "تحصيل حسابات" من المستخدم)، (2) ميزات أمان إنتاج (SSL pinning, mock location) مؤجلة عمداً لحد ما تكون فيه شهادات حقيقية، (3) أي تحسينات/ميزات إضافية هتظهر لما نستخدم النظام فعلياً ونلاقي حاجات ناقصة (طبيعي في أي نظام حقيقي).

---

## 6. الخطوات الجاية بالظبط (لو الجلسة اتقطعت، ابدأ من هنا)

بالترتيب:

1. ✅ الملف ده.
2. إعدادات نقاط الولاء (`0043_loyalty_settings.sql` + ربط `LoyaltyService.earn()` بـ`settleAndComplete()` في `payments.service.ts` باستخدام `SettingsService.getNumber()`) — التصميم اتقرر (§4.2)، لسه التنفيذ.
3. صلاحية شات الدعم لـ`ops_manager` (migration جديدة تمنح `support_tickets.manage`).
4. `GET /admin/wallets/:userId` + عرضها في صفحتي العميل/الفني في `apps/admin`.
5. الشات الداخلي (`internal_chat_threads`/`internal_messages` — migration + entities + service + controller + شاشة في `apps/admin` + شاشة في `apps/technician-app`).
6. تشغيل الفحوصات الثلاثة (`tsc`/`nest build`/`jest`) + اختبار حي لكل حاجة فوق + تحديث الـ READMEs المعنية + commit + push.
7. تحديث القسم ده والقسم اللي فوقه (§4، §5) بعلامة ✅ بدل ⏳، وبعدين كمّل أي فجوة تانية تكتشفها بنفس منهجية "كمل بدون توقف" المعتادة.

---

## 7. حاجات محتاجة منك (مفاتيح/قرارات خارجية) — ملخّص سريع

التفاصيل الكاملة بالخطوات في `docs/03-external-integrations.md`. باختصار شديد:

| الخدمة | المتغيرات | الحالة |
|---|---|---|
| Paymob (دفع بطاقة) | `PAYMOB_API_KEY`, `PAYMOB_INTEGRATION_ID_CARD`, `PAYMOB_IFRAME_ID`, `PAYMOB_HMAC_SECRET` | ❌ مش متحطة |
| FawryPay (دفع كود مرجعي) | `FAWRY_MERCHANT_CODE`, `FAWRY_SECURE_KEY` | ❌ مش متحطة + توقيع محتاج تأكيد من التوثيق الرسمي قبل إنتاج |
| S3/تخزين صور | `S3_*` (أو أي بديل متوافق) | ❌ مش متحطة (بيستخدم تخزين محلي فعلاً دلوقتي) |
| FCM (إشعارات push) | `FIREBASE_SERVICE_ACCOUNT_JSON` + `google-services.json`/`GoogleService-Info.plist` | ❌ مش متحطة |
| Twilio (SMS/WhatsApp) | `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_SMS_FROM_NUMBER`, `TWILIO_WHATSAPP_FROM_NUMBER` | ❌ مش متحطة |
| SMTP (إيميل) | `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD` | ❌ مش متحطة |
| Google Maps | مفتاح Android + مفتاح iOS | ❌ مش متحطة |

**كله log-only/رافض بوضوح دلوقتي، مفيش أي حاجة بتتعطل أو بتتظاهر إنها شغالة وهي لأ.**

---

## 8. دروس/قرارات مهمة تستاهل تفتكرها

- **مبدأ حاكم في كل المشروع**: أي فشل في خدمة خارجية (Redis، queue، بوابة دفع، SMS) لازم يتلقّط ويتعامل معاه بأمان — أبداً ميكسرش أو يعلّق عملية حقيقية للمستخدم. الدرس اللي اتعلّمناه بالتجربة: `queue.add()` كانت بتعلّق طلب حقيقي دقايق وقت انقطاع Redis قبل ما نكتشف ونصلح.
- **بَقّة موثّقة معروفة، مش قابلة للإصلاح من كودنا**: BullMQ Worker مش بيرجع يعالج وظايف بعد انقطاع Redis طويل من غير إعادة تشغيل الـ process (مطابقة لـ GitHub issue #4479 في BullMQ نفسه). موثّقة في `technicians/README.md` و`ratings/README.md` و`matching/README.md`.
- **مفيش اختراع أرقام عمل (business numbers) من غير أساس**: أي رقم زي "معدل كسب نقاط الولاء" أو "خوارزمية ترقية فني تلقائية" اتأجّل لحد ما يتحدد رسمياً أو (زي نقاط الولاء دلوقتي) يتحط كإعداد قابل للتغيير بقيمة افتراضية معقولة بدل قيمة مُختلَقة ثابتة في الكود.
- **بيئة العمل دي (sandbox) محدودة**: مفيش جهاز/إيموليتور Android أو iOS حقيقي (كل اختبار UI بصري لتطبيقات Flutter بيتم عبر `flutter analyze` + منطق مُتحقق منه حياً بس مش رؤية بصرية فعلية)، والبروكسي بيمنع الوصول لدومينات مش على قايمة سماح (بوابات دفع حقيقية، خرائط تايلز، إلخ) — موثّق بالتفصيل في كل README متأثر.
- **اتفاقية الـ commits**: كل ميزة = commit منفصل، رسالة عربية موجزة، push فوري لـ`claude/home-services-app-plan-v13gb2`. الفحوصات الثلاثة (`tsc`/`nest build`/`jest`) لازم تعدّي قبل أي commit في `apps/api`. `flutter analyze` لازم يعدّي نضيف قبل أي commit في التطبيقين.
