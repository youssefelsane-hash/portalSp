# apps/api — NestJS Backend

الـ Backend الأساسي (Modular Monolith). كل موديول جوّه `src/modules/*` وله حدود مستقلة (راجع §2.4 في الماستر بلان).

الستاك: NestJS + PostgreSQL 16 + PostGIS + Redis + BullMQ + Socket.io.
لسه معملش `npm init` — ده أول خطوة في السبرنت S1 من المرحلة 1 (MVP).

مرجع كامل: `../../docs/01-master-plan.md`
