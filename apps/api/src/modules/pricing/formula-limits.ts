// حدود محرك المعادلات — مصدر واحد مشترك بين الباك-إند (formula-evaluator.ts وقت الحفظ/
// التنفيذ) وواجهة الأدمن (formula-tree-editor.tsx وقت البناء) عشان مايبقاش فيه رقمين مختلفين
// للأدمن والـAPI (كانت بالظبط الفجوة اللي المهمة حذّرت منها: "المحرر يبني 48 والـAPI يرفض 12").
//
// الاختيارات:
// - DEPTH 48: طلب صريح من المالك (docs/01B §2) — مع تعقيد محدود بطرق تانية تحت.
// - NODES 1500 لكل payload كامل (كل المخرجات مجتمعة): العمق لوحده مش بيكفي كحماية DoS —
//   شجرة عريضة (operands كتير) ممكن تكون مليون عقدة بعمق 2. 1500 كتير جدًا لأي معادلة
//   واقعية (الأعمق الحالي في الداتابيز < 20 عقدة) وصغيرة على المحرك.
// - PAYLOAD_BYTES 128KB: نفس فلسفة حدود HTTP payloads — JSON أكبر من كده مش معادلة، ده
//   ملف مخفي.

export const FORMULA_LIMITS = {
  /** أقصى عمق متداخل مسموح لأي شجرة معادلة. */
  MAX_DEPTH: 48,
  /** أقصى عدد عقد إجمالي في payload معادلة final_price كاملة (كل المخرجات). */
  MAX_NODE_COUNT: 1500,
  /** أقصى حجم JSON بالبايت للـpayload المخزن. */
  MAX_PAYLOAD_JSON_BYTES: 128 * 1024,
} as const;
