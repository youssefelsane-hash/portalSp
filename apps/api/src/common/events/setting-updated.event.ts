/**
 * إعداد أدمن اتغيّر (§33) — بدائي عام مش خاص بإعداد معيّن، أي موديول ممكن يستمع له عشان يعيد
 * تحميل قيمة محفوظة في الذاكرة (زي `InstaPayProvider`) بدل ما يعتمد على قراءة `SettingsService`
 * في كل نداء.
 *
 * **النطاق: النسخة الحالية بس (in-process EventEmitter2).** ده **مش عيب** لمعظم المستمعين —
 * الحدث ده بيتطلق على النسخة اللي الأدمن عمل عليها `PATCH`، ومعالج زي بث الأدمن اللحظي لازم
 * يشتغل **مرة واحدة** فعلاً (البث نفسه بيوصل لكل النسخ عبر Socket.IO Redis adapter — ADR-0073).
 *
 * الاستثناء الوحيد: **الخدمات اللي ماسكة نسخة من الإعداد في ذاكرتها** (بوابات الدفع). دي على
 * نسخة تانية مش هتسمع الحدث خالص، فتفضل شغّالة **بمفاتيح قديمة** لحد ما الـprocess يترستِرت —
 * وده يعني نص حركة الدفع على بوابة اتلغى إعدادها. للحالة دي بالظبط فيه
 * `SETTING_RELOAD_REQUIRED_EVENT` تحت، و`SettingsCrossInstanceBridge` بينقله بين النسخ.
 */
export const SETTING_UPDATED_EVENT = 'setting.updated';

/**
 * **«إعداد اتغيّر على نسخة تانية — اقرا تاني أي حاجة ماسكها في الذاكرة»** (تدقيق A-3).
 *
 * اسم منفصل عن `SETTING_UPDATED_EVENT` **عن عمد**، مش ترفًا:
 *
 * لو الجسر أعاد إطلاق `SETTING_UPDATED_EVENT` نفسه على النسخ التانية، كان
 * `AdminRealtimeGateway` هيبثّ التغيير للأدمن **مرة لكل نسخة** — يعني إصلاح فجوة بإدخال بَقّة
 * تكرار مكانها. الفصل بالاسم بيخلي الاشتراك **قرار صريح**: الخدمة اللي ماسكة حالة في الذاكرة
 * بتضيف الديكوريتور ده بنفسها، وأي مستمع تاني (بث/إشعار/كتابة في القاعدة) مابيتأثرش أصلاً.
 *
 * القاعدة اللي بتحكم الاشتراك: **اشترك هنا لو ومتى كان معالجك idempotent وأثره في ذاكرة النسخة
 * دي بس.** أي معالج بيكتب في القاعدة أو بيبعت إشعار أو بيبثّ لعميل **مايشتركش** — تنفيذه مرة
 * واحدة على نسخة الأدمن هو **السلوك الصح**، وتكراره على كل نسخة هو البَقّة.
 */
export const SETTING_RELOAD_REQUIRED_EVENT = 'setting.reload_required';

export class SettingUpdatedEvent {
  constructor(
    public readonly key: string,
    public readonly value: unknown,
  ) {}
}
